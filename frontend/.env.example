# ============================================
# SMART EXPORT — Variables d'environnement
# ============================================

# --- APIs GRATUITES (pas de clé requise) ---
# restcountries.com, flagcdn.com, exchangerate.host → pas de clé

# --- Backend Spring Boot ---
VITE_API_BASE_URL=http://localhost:8080/api

# Maritime backend endpoints (if deployed separately)
VITE_MARITIME_API_BASE_URL=http://localhost:8080/api/maritime

# Optional PIN configuration (demo defaults are in code)
VITE_USER_PIN=
VITE_ADMIN_PIN=

# --- Environment ---
VITE_ENV=development
